spring.application.name=api-user
# Conexi�n a Base de Datos (Docker)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}

# Comportamiento de JPA/Hibernate
# 'update': crea/actualiza las tablas autom�ticamente seg�n tus Entidades
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


springdoc.swagger-ui.supported-submit-methods=
springdoc.swagger-ui.default-models-expand-depth=-1